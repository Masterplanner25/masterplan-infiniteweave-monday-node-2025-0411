You are performing a FINAL PLATFORM PURITY SWEEP.

Focus ONLY on:

    AINDY/

---

## OBJECTIVE

Ensure AINDY/ contains ONLY:

- runtime logic
- platform infrastructure
- system primitives

And NOTHING from:

- apps/
- product logic
- domain/business behavior

---

## DEFINITIONS

### VALID inside AINDY/
- runtime/
- platform_layer/
- memory/
- watcher/
- db/
- kernel/
- core/
- sdk/

---

### INVALID inside AINDY/
- business logic
- app-specific services
- product-specific schemas
- domain-specific naming (masterplan, SEO, rippletrace, etc.)
- direct imports from apps/

---

## TASK

---

# 1. IMPORT SCAN (CRITICAL)

Search entire AINDY/ for:

    from apps.
    import apps.

List all occurrences.

These are violations.

---

# 2. DOMAIN LANGUAGE SCAN

Search for:

- masterplan
- rippletrace
- seo
- freelance
- tasks
- analytics
- authorship
- search (if domain-specific)

Identify files containing domain-specific logic.

---

# 3. SERVICE LAYER CHECK

Inspect:

- platform_layer/
- runtime/
- core/
- kernel/

Find any:

- business logic
- domain-specific calculations
- feature-specific behavior

---

# 4. DATA MODEL CHECK

Inspect:

- AINDY/db/models/

Ensure:

- models are platform-level only
- no app-specific entities remain

---

# 5. SCHEMA CHECK

Inspect:

- AINDY/schemas/

Ensure only platform schemas remain.

---

# 6. MEMORY / WATCHER CHECK

Verify:

- no app-specific logic inside memory/
- no domain-specific logic inside watcher/

---

# 7. UNUSED / LEGACY CODE

Find:

- files not imported anywhere
- legacy helpers
- dead modules

---

# 8. CLASSIFY ALL VIOLATIONS

For each violation:

- move to apps/
- move to tools/
- delete
- keep (if justified)

---

# 9. FINAL VERDICT

Choose ONE:

- "AINDY is clean platform/runtime"
- "Minor residual app logic remains"
- "Significant coupling still exists"

---

# 10. OUTPUT

Provide:

- list of violations
- recommended actions
- confidence level (0–100%)

---

## IMPORTANT

- DO NOT move files yet
- DO NOT refactor
- ONLY identify violations

This is the final architectural validation.



 Final Verdict

  Significant coupling still exists.

  I did not move or refactor anything. This was an audit-only sweep of AINDY/.

  Direct apps.* Imports

  These are the only direct app imports found inside AINDY/:

  - AINDY/db/model_registry.py:44 imports apps.analytics.models
  - AINDY/db/model_registry.py:45 imports apps.arm.models
  - AINDY/db/model_registry.py:46 imports apps.authorship.models
  - AINDY/db/model_registry.py:47 imports apps.automation.models
  - AINDY/db/model_registry.py:48 imports apps.freelance.models
  - AINDY/db/model_registry.py:49 imports apps.masterplan.models
  - AINDY/db/model_registry.py:50 imports apps.rippletrace.models
  - AINDY/db/model_registry.py:51 imports apps.search.models
  - AINDY/db/model_registry.py:52 imports apps.tasks.models

  These are side-effect imports for SQLAlchemy metadata registration, but they still violate the strict rule. Recommended action: replace with a plugin/
  entrypoint-style model registration mechanism or app-owned registration called from bootstrap/Alembic setup.

  Product Logic / Domain Leakage

  - AINDY/runtime/flow_engine.py:698 still calls _update_goals_from_execution(...) on failure paths. This is a clear runtime-to-domain leak and likely a
    latent bug if the helper was removed.
  - AINDY/runtime/nodus_schedule_service.py:71 uses get_job("tasks.background.is_leader"). Registry-based, but still hardcodes an app-specific job name in
    runtime.
  - AINDY/main.py:272 uses get_job("tasks.background.start").
  - AINDY/main.py:416 uses get_job("tasks.background.stop").
  - AINDY/worker.py:15 keeps task-service compatibility naming and tasks.background.* jobs.
  - AINDY/worker__init__.py:21 keeps the same task-background job coupling.
  - AINDY/platform_layer/master_index_service.py:1 contains calculate_master_index(tasks), a product-style calculation placeholder. Move to apps or delete.
  - AINDY/core/router_guard.py:39 embeds services.analytics_service.
  - AINDY/core/router_guard.py:101 embeds app router migration names including analytics, SEO, RippleTrace, and authorship.
  - AINDY/core/execution_unit_service.py:315 exposes view_from_task(task), mapping app task fields inside core.
  - AINDY/core/execution_pipeline.py:25 defaults route prefix behavior to "task".
  - AINDY/core/execution_pipeline.py:834 contains app router/domain names in comments.
  - AINDY/routes/observability_router.py exposes RippleTrace-specific route naming. Move to apps/rippletrace or rename to generic trace graph semantics.

    domain language. Rename to ExecutionStrategy or FlowStrategy if it is truly platform-level.
  - AINDY/schemas/ appears clean. It contains platform/auth schemas only.

  Memory / Watcher

  - AINDY/memory/memory_capture_engine.py:43 contains "masterplan_locked": 1.0. Move this significance rule to an app-level memory policy registry.
  - AINDY/memory/memory_helpers.py contains domain examples such as ARM analysis and lead search. Documentation/example leakage, but still worth
    neutralizing.
  - AINDY/memory/memory_address_space.py contains examples with tasks/goals-style namespaces. Recommended: replace examples with neutral platform terms.
  - AINDY/watcher/ is mostly clean; only README/example language appears task-oriented.

  Structural / Legacy Issues

  - AINDY/memory/native/memory_bridge_rs/target/ contains Rust build artifacts under platform source. Delete from source and ignore.
  - AINDY/__pycache__/, AINDY/.pytest_cache/, and AINDY/.ruff_cache/ should not live in tracked platform source.
  - AINDY/sdk/tests/test_sdk.py is test code inside AINDY/; if enforcing the earlier test normalization rule, move it under root tests/.
  - AINDY/.github/, AINDY/scripts/, AINDY/services/, AINDY/routes/, and AINDY/worker/ are outside the stated valid directory list. Some are platform-
    adjacent, but they need either relocation or explicit architectural justification.

  Recommended Actions

  1. Replace AINDY/db/model_registry.py hardcoded app imports with app/plugin model registration.
  2. Remove the remaining goals failure-path call from flow_engine.py; emit a generic execution failure event instead.
  3. Replace tasks.background.* job names in main.py, worker.py, and nodus_schedule_service.py with generic lifecycle/scheduler hooks.
  4. Move master_index_service.py, task projections, and RippleTrace routes to app-owned modules.
  5. Remove platform model relationships that point to app models, especially User.tasks.
  6. Move domain-specific memory rules and examples to app memory policy modules.
  7. Delete caches/build artifacts and move SDK tests to root tests/.

  Confidence: 88%. The major coupling points are clear from direct scans. Residual risk is mostly around ambiguous terms like task, goal, strategy, and
  search, where some uses are generic platform language and others are app-domain leakage.


