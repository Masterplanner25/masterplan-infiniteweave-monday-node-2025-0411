# Archive

This directory holds retired code and historical artifacts that are kept for reference only.

`archive/legacy/` is the former `legacy/` tree moved out of the active package surface during V1 cleanup.
